import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  errorText: {
    fontWeight: "bold",
    color: "red",
    textAlign: "center",
    paddingBottom: 20,
    marginTop: 10,
  },
});
